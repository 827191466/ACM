#include<iostream>
using namespace std;
int main()
{
	int n[10000],left,right;
	double mid;
	left=0;
	right=10000;
	while(left<=right){
		mid=(right+left)/2;
		
	}
	lower_bound(a,a+10,4)-a//第一个小于等于 
	upper_bound(a,a+10,4)-a//第一个大于等于 
	binary_search(a,a+10,4)//二分查找
	equal_range(a,a+10,4) //返回大于等于 返回小于等于 
	return 0;
}

