//完全背包
for (i = 0; i < n; i++) {
	for (j = 0; j <= v; j++) {
		dp[j] = max(dp[j], dp[j - c[i]] + v[i]);
		}
}


//分组背包
vector<pait<int, int>>a[k];
vector<int>g[N];

a[i].push_back(make_pair(c1, v1));
for (i = 0; i < k;i++) {
	for (j = v;j > 0,j--) {
		for(k1=0;k1<a[i].size<<k1++)
	}
}